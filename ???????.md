# 流水账 · 发布与更新指南

## 一、网址（永远不变）

> https://xion-darren.github.io/ledger/

手机、电脑、平板都打开这个网址使用。请收藏到浏览器书签。

## 二、整套系统由哪些部分组成

| 部分 | 位置 | 作用 |
|---|---|---|
| 程序（网页） | 代码仓库 `XION-Darren/ledger`（公开，GitHub Pages 托管） | 所有设备访问的界面 |
| 数据（我的账本） | 私有仓库 `XION-Darren/ledger-data` | 我的记录，仅我可见 |
| 数据（家人账本） | 私有仓库 `XION-Darren/Hledger-data` | 家人记录，可邀请家人协作用 |
| 数据（本地） | 各设备浏览器 localStorage | 主存储，离线也能记 |
| 开发文件 | 本机文件夹 `TransLedger\` | 改功能的唯一入口 |
| 历史版本 | 本机 `TransLedger\versions\` + 代码仓库 `versions/` | 找回旧版本用 |

## 三、日常使用

1. 打开网址，正常记录即可
2. 每台设备**首次**使用时：设置页 → 填 Access Token → 点「测试连接并保存」（仓库信息已预填）
3. 两个人各用各的账本：点「快速切换数据仓库」选「我的账本」或「家人账本」
4. 以后每次打开页面自动拉取云端数据；记一笔后自动推送

## 四、如何更新程序版本（发布新功能）

### 推荐方式：一键发布脚本 publish.ps1

**脚本在哪**：`C:\Users\22951\Desktop\戚庆彬\TransLedger\publish.ps1`

```
powershell -ExecutionPolicy Bypass -File "C:\Users\22951\Desktop\戚庆彬\TransLedger\publish.ps1" -Version v1.1 -Message "本次更新说明"
```
- `-Version`：可选，填了则自动归档新版本（本地 versions\ + 仓库 versions/）
- `-Message`：本次改动说明

运行后等 1~3 分钟，刷新网址即可看到新版本。

### 版本号规则
- 核心功能/重构：v1.x → v2.x（升大版本）
- 修复/小改动：v1.0 → v1.1（升小版本）

### 重要原则
- 网址永远不变，书签不用改
- 只改程序代码，**不要动**数据仓库和 token 配置，数据不受影响

## 五、多设备使用 / 换新电脑

### 换设备（日常使用，不需要任何文件）
1. 新设备浏览器打开网址
2. 设置页填 Access Token → 测试连接并保存
3. 数据自动同步过来，无需复制任何文件

### 换电脑继续开发（复制文件夹）
把整个 `TransLedger` 文件夹复制到新电脑即可，它包含：代码、versions\、publish.ps1、publish-config.json、AGENTS.md。
新电脑上用同样的方式运行发布脚本即可继续开发维护。详见《更换设备重新部署流程操作说明.txt》。

## 六、数据备份（保险起见）

- 设置页「数据备份」→「导出 JSON 备份」，可随时手动下载全部数据
- 云端两个私有数据仓库本身就是备份
- 代码仓库里的 `versions/` 是程序代码的历史备份（不含数据）

## 七、安全提醒（重要）

- `publish-config.json`、`TOKENS` 文件内含 Token：**绝不能上传到 GitHub 或发给任何人**
- 若某 Token 泄露：到 github.com/settings/tokens 删除重建，然后更新 publish-config.json 和网页设置里的值
- 网页里的 Token 只存在浏览器 localStorage，不进代码、不进仓库
